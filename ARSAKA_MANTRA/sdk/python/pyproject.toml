[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mantra-sdk"
version = "0.1.0"
description = "Python SDK for ARSAKA_MANTRA Decision System"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "ATLAS Team" }
]
keywords = [
    "mantra",
    "decision",
    "architecture",
    "governance",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.25.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-httpx>=0.22.0",
    "mypy>=1.0.0",
]

[project.urls]
Documentation = "https://github.com/atlas/mantra-sdk"
Repository = "https://github.com/atlas/mantra-sdk"

[tool.hatch.build.targets.wheel]
packages = ["mantra_sdk"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_ignores = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
