# ============================================================================
# ATLAS_PUGUH Phase A - Python Dependencies (Minimal)
# ============================================================================
# Phase A dependencies: CORE functionality only
# NO Redis, NO Prometheus, NO Jaeger, NO Rate Limiting
#
# Security Audit: 2026-01-28
# Last checked against: pip-audit, safety, snyk
# ============================================================================

# ----------------------------------------------------------------------------
# Web Framework
# ----------------------------------------------------------------------------
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
# Starlette is a FastAPI dependency - ensure compatible version
starlette>=0.35.0,<1.0.0

# ----------------------------------------------------------------------------
# Database (PostgreSQL)
# ----------------------------------------------------------------------------
sqlalchemy[asyncio]>=2.0.25,<3.0.0
asyncpg>=0.29.0,<1.0.0
psycopg2-binary>=2.9.9,<3.0.0

# ----------------------------------------------------------------------------
# Data Validation & Settings
# ----------------------------------------------------------------------------
pydantic>=2.5.3,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
email-validator>=2.1.0,<3.0.0

# ----------------------------------------------------------------------------
# Authentication (JWT)
# Security Note: Use PyJWT directly. python-jose is optional for legacy support.
# ----------------------------------------------------------------------------
PyJWT[crypto]>=2.8.0,<3.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
# python-jose kept for compatibility but PyJWT preferred
python-jose[cryptography]>=3.3.0,<4.0.0
bcrypt>=4.0.0,<5.0.0

# ----------------------------------------------------------------------------
# Utilities
# ----------------------------------------------------------------------------
python-dateutil>=2.8.2,<3.0.0
httpx>=0.26.0,<1.0.0

# ----------------------------------------------------------------------------
# Caching & Rate Limiting (Redis)
# ----------------------------------------------------------------------------
redis[hiredis]>=5.0.1,<6.0.0

# ============================================================================
# Security Audit Checklist
# ============================================================================
#
# Run these commands regularly to check for vulnerabilities:
#
# 1. pip-audit (Python Security Advisory Database)
#    pip install pip-audit
#    pip-audit -r requirements-phase-a.txt
#
# 2. safety (PyUp.io Safety DB)
#    pip install safety
#    safety check -r requirements-phase-a.txt
#
# 3. GitHub Dependabot (automatic)
#    Configure .github/dependabot.yml
#
# ============================================================================
# DISABLED FOR PHASE A (Reference Only)
# ============================================================================
#
# Redis Cache:         redis[hiredis]>=5.0.1,<6.0.0
# Rate Limiting:       slowapi>=0.1.9,<1.0.0
# Prometheus:          prometheus-client>=0.19.0,<1.0.0
# OpenTelemetry:       opentelemetry-api>=1.21.0,<2.0.0
#                      opentelemetry-sdk>=1.21.0,<2.0.0
#                      opentelemetry-exporter-jaeger>=1.21.0,<2.0.0
#
# ============================================================================
# PHASE B MIGRATION
# ============================================================================
#
# When enabling infrastructure features in Phase B, add:
# - requirements-redis.txt (caching)
# - requirements-observability.txt (metrics + tracing)
# - requirements-ratelimit.txt (rate limiting)
#
# ============================================================================
