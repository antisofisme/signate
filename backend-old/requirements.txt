# =============================================================================
# SMART TV DIGITAL SIGNAGE - BACKEND API DEPENDENCIES
# =============================================================================
# Python 3.11+
# Install: pip install -r requirements.txt
# =============================================================================

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0  # ASGI server with auto-reload

# Database
sqlalchemy==2.0.25  # ORM
psycopg2-binary==2.9.9  # PostgreSQL adapter
alembic==1.13.1  # Database migrations (optional)

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.1.2  # Compatible with passlib 1.7.4
python-multipart==0.0.6  # Form data parsing (file upload)
cryptography==42.0.0  # Encryption for sensitive data

# HTTP Client (for Anthias & Firebird API)
httpx==0.26.0  # Async HTTP client

# Firebird Database
fdb==2.0.2  # Firebird database driver

# Redis (Cache & Session)
redis==4.6.0  # Redis client (compatible with Celery 5.3.4)
hiredis==2.3.2  # Redis performance boost

# Celery (Background Tasks & Video Transcoding)
celery[redis]==5.3.4  # Distributed task queue
kombu==5.3.4  # Messaging library for Celery
vine==5.1.0  # Promises/Deferreds for Celery

# WebSocket support
websockets==12.0  # WebSocket protocol

# Environment & Config
python-dotenv==1.0.0  # Load .env files
pydantic==2.5.3  # Data validation
pydantic-settings==2.1.0  # Settings management
email-validator==2.1.0  # Email validation for Pydantic

# Utilities
python-dateutil==2.8.2  # Date/time utilities
pytz==2023.3  # Timezone handling

# File Handling
python-magic==0.4.27  # File type detection (optional)
pillow==10.2.0  # Image processing (optional)

# Video Processing
ffmpeg-python==0.2.0  # FFmpeg wrapper for video processing

# Logging
loguru==0.7.2  # Better logging (optional)
python-json-logger==2.0.7  # JSON structured logging for production

# Testing (Development)
pytest==7.4.4  # Testing framework
pytest-asyncio==0.23.3  # Async test support
faker==22.0.0  # Generate fake data for testing

# Development Tools
black==23.12.1  # Code formatter
flake8==7.0.0  # Linter
mypy==1.8.0  # Type checker
